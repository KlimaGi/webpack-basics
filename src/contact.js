import React from "react";
import { render } from "react-dom";

const Contact = () => {
  return (
    <div>
      <h2>Contact webpack</h2>
    </div>
  );
};

render(<Contact />, document.getElementById("target"));
